<Page x:Class="Protego.Pages.Page1"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Protego.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Page1">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Margin="5">
            <Button>
                <!-- Content to be clicked -->
                <StackPanel Margin="10">
                    <TextBlock Text="Clickable Content" FontWeight="Bold" FontSize="16"/>
                    <TextBlock Text="Click me!" Foreground="Blue" Margin="0,5,0,0"/>
                </StackPanel>
            </Button>
        </Border>
    </Grid>
</Page>
